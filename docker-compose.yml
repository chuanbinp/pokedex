version: "3.8"
services:
    frontend:
        build: frontend
        image: chuanbinphoe/pokedex-frontend
        restart: always
        ports:
        - "3000:3000"

    backend:
        build: backend
        image: chuanbinphoe/pokedex-backend
        restart: always
        ports:
        - "5000:5000"